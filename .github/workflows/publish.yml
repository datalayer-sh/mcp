name: Publish to npm

on:
  push:
    branches: [main]
    paths:
      - 'package.json'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Check if version changed
        id: version
        run: |
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          NPM_VERSION=$(npm view @datalayer-sh/mcp version 2>/dev/null || echo "0.0.0")
          if [ "$PACKAGE_VERSION" = "$NPM_VERSION" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "Version $PACKAGE_VERSION already published, skipping."
          else
            echo "skip=false" >> $GITHUB_OUTPUT
            echo "Publishing $PACKAGE_VERSION (npm has $NPM_VERSION)"
          fi

      # Auth is via npm Trusted Publisher (OIDC). No NODE_AUTH_TOKEN is set —
      # `id-token: write` in the job permissions lets npm verify provenance
      # and authenticate this workflow against the registry trust policy
      # configured at https://www.npmjs.com/package/@datalayer-sh/mcp/access.
      - name: Publish to npm (OIDC trusted publisher)
        if: steps.version.outputs.skip == 'false'
        run: npm publish --access public --provenance
