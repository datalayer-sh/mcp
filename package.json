{
  "name": "@datalayer/mcp",
  "version": "0.1.0",
  "description": "DataLayer MCP Server — B2B enrichment for AI agents. 60M companies, 300M contacts, intent signals.",
  "bin": { "datalayer-mcp": "./dist/index.js" },
  "main": "./dist/index.js",
  "type": "module",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["mcp", "model-context-protocol", "b2b", "enrichment", "datalayer", "ai-agent", "claude", "cursor"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/blink-new/datalayer.git",
    "directory": "mcp"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
